'use strict';

const NETWORK_ASSERTATION_ERROR = 'network must be a string';

const LOCALHOST = '127.0.0.1';
const DEFAULT_NETWORK = 'Wi-Fi';
const DEFAULT_PORT = 9050;

const OK = 'OK';

module.exports = {
  NETWORK_ASSERTATION_ERROR,
  LOCALHOST,
  DEFAULT_NETWORK,
  DEFAULT_PORT,
  OK,
};

